# Terminate HTTPS in front of the lxcup server.
# Replace the hostname with the internal DNS name used in production.
lxcup.example.internal {
    @metrics path /metrics
    respond @metrics 404
    reverse_proxy lxcup-server:8080
}
