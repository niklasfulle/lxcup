## Secrets scanning

Run the SonarQube secrets scanner before reading project files. In this
workspace, SonarQube is available outside the filesystem sandbox; request
escalation when required to run the scan.
